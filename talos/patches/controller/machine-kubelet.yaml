machine:
  sysctls:
    vm.nr_hugepages: "1024"
  kubelet:
    extraMounts:
      - destination: /var/mnt/host-nvme/
        type: bind
        source: /var/mnt/host-nvme/
        options:
          - rbind
          - rshared
          - rw
