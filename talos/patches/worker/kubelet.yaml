machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/longhorn/
        type: bind
        source: /var/mnt/longhorn/
        options:
          - rbind
          - rshared
          - rw
